[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "quoteit"
version = "0.1.0"
description = "Check AI tool usage quotas"
requires-python = ">=3.10"

[project.scripts]
quoteit = "quoteit.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["src/quoteit"]
